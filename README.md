Submission for the 2026 IEEE WCCI MicroRTS LLM Game AI Competition.

This repository mirrors the contents of `ai4pc_submission.zip` — Java agent (`AI4PC.java`), Python proxy (`proxy.py`, `router.py`), compiled engine (`microrts.jar`), benchmark output, and raw logs.
